# KDAF 复现 · 环境说明与检查摘要（中文对照版）

> 供作者 review 用，2026-08。本文件伴随 **Lunyakin (2026), "Auditable by Construction"
> (arXiv:2608.20661)** 的独立复现。所有哈希与指标均来自记录在案、CPU-only 的本地运行。
> 中文版为工作对照用，正式交付请用英文版 `environment-notes-inspection-summary.md`。

---

## 1. 环境

| 组件 | 作者（记录值） | 我们（复现值） |
|---|---|---|
| 操作系统 | macOS (arm64) | Windows 11 (x64) |
| Python | CPython 3.13.3 | CPython 3.13.12 (venv) |
| NumPy / pandas | numpy 2.4.3, pandas 2.3.3 | 同（来自 requirements.txt） |
| transformers | 4.57.6 | 4.57.6 |
| 推理硬件 | GPU | 纯 CPU |
| 作答模型 | Qwen3-8B，digest 前缀 `500a1f067a9f` | 同 digest |
| 裁决模型 | llama3:latest（`365c0bd3c000`） | 同 digest |
| tokenizer | Qwen/Qwen3-8B rev `b968826d9c46...` | 同固定 revision，本地缓存 |

全程在由 `environment/requirements.txt` 建的 Python 3.13 venv 内运行，环境变量
`HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1` 全程开启。

---

## 2. 所需环境适配（Windows / CPU-only）

共 4 处，均不改作者源码行为：

1. **`resource` 模块 shim**——Windows 无 Unix `resource` 模块，用 `win_wrapper.py`
   注入 `getrlimit`/`setrlimit` 桩。
2. **本地 tokenizer 缓存**——代码以 `local_files_only=True` 加载 Qwen3-8B tokenizer，
   用 HF CLI 把固定 revision 缓存到本地。
3. **离线 transformers**——`HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`：transformers
   4.57.6 在 tokenizer 初始化时发一次网络调用，绕过了 `local_files_only`。
4. **Windows symlink 修复**——HF CLI 解压后 snapshot 文件为 0 字节（Windows 软链接失效），
   把底层 blobs 复制成 snapshot 布局修复。

---

## 3. 数据层校验（SHA-256，全部 PASS）

| 产物 | SHA-256 | 状态 |
|---|---|---|
| 评测集（145 行） | `393707d2ff...` | PASS |
| 源索引（189 项） | `6f6f17b218...` | PASS |
| 规范图谱（524/1852） | `238f4e8807...` | PASS |

---

## 4. 检索层核对（M1–M8，全部逐位一致）

| 指标 | 我们 | 作者（definitive） | 结果 |
|---|---|---|---|
| 跨公司泄漏 — graph_ontology | 0.0000 | 0.0000 | ✅ 精确相等 |
| 跨公司泄漏 — graph_no_ontology | 0.0000 | 0.0000 | ✅ |
| 跨公司泄漏 — bm25_text | 0.1678 | 0.1678 | ✅ |
| 跨公司泄漏 — hybrid | 0.2023 | 0.2023 | ✅ |
| 期外证据 — graph_ontology | 0.2053 | 0.2053 | ✅ |
| 期外证据 — graph_no_ontology | 0.2815 | 0.2815 | ✅ |
| 期外证据 — bm25 / hybrid | 0.2299 / 0.2529 | 0.2299 / 0.2529 | ✅ |
| 血缘解析失败（全条件） | 0 | 0 | ✅ |
| 重放保真（全条件） | 145/145 | 145/145 | ✅ |
| hop 分布 — graph_ontology | {1:367, 3:59} | {1:367, 3:59} | ✅ |
| hop 分布 — graph_no_ontology | {3:424} | {3:424} | ✅ |
| 图规模 | 524 节点 / 1852 边 | 同 | ✅ |
| 上下文 token — graph_ontology | mean 1581.4 / p50 1551 | 同 | ✅ |
| 上下文 token — bm25_text | mean 1510.8 / p50 1423 | 同 | ✅ |

---

## 5. 生成层结果（seed 42, 145 题, Qwen3-8B）

| 系统 | 可追溯性 F1（我们 / 作者） | 正确率（我们 / 作者） | 在作者 CI 内？ |
|---|---|---|---|
| graph_ontology (KDAF) | 0.5147 / 0.5147 | 0.1241 / 0.1103 | 是 [0.069, 0.172] |
| graph_no_ontology | 0.4876 / 0.4876 | 0.1172 / 0.1034 | 是 [0.062, 0.159] |
| bm25_text | 0.4625 / 0.4625 | 0.1310 / 0.1172 | 是 [0.075, 0.180] |

核心对比精确复现：**KDAF − BM25 = +0.0522** 可追溯性，作者为 **+0.052**；
正确率差（−0.007）在报告置信区间内。

---

## 6. 文档缺口：Qwen3 思考模式（需向作者指出）

我们所用的一行生成 payload **未显式包含 `"think": False`**。在 Ollama 下，Qwen3
对复杂问题会进入推理模式，耗尽 `num_predict` 预算（256）并返回空响应。在生成
payload 顶层加一个 `"think": False`（`mpr_a/llm.py` 一行改动）即复现作者 no-think
行为与记录指标。no-think 配置在作者环境中是隐式的，因此未出现在 artifact README
中——我们在此作为可复现性修复标注，并建议作者在文档中写明。