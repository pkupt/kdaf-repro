# Exclude data, models, caches — never push benchmark content or model weights
data/
.cache/
*.pdf
*.log
*.zip
*.jsonl
# exception: per-question event logs are deliverable (Reproducibility Statement (c))
!results/events/*.jsonl
__pycache__/
*.pyc
.venv/
.ollama/
